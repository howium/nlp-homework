# virtualenv (shared, at repo root)
.venv/

# ---- per-homework generated / large artifacts (regenerable) ----
# hw1: data splits (regenerated by hw1/src/data_prep.py with fixed seed)
hw1/data/nyt_train.csv
hw1/data/nyt_val.csv
hw1/data/nyt_test.csv
# hw1: downloaded resources (GloVe)
hw1/data/*.zip
hw1/data/*.txt
# hw1: model artifacts (BERT weights ~440MB, Word2Vec models, trainer output)
hw1/models/
# hw1: report build intermediates (chart is rebuilt by the report script)
hw1/report/body.pdf
hw1/report/cover.pdf
hw1/report/chart_methods.png

# ---- generic patterns for future homework ----
hw*/data/*.zip
hw*/data/*.txt
hw*/models/

# misc
.DS_Store
__pycache__/
*.log

# local ZCode workspace config (tool-specific, keep local only)
.zcodeignore

# report build tooling (local only, not part of the homework code)
hw1/src/generate_report_pdf.py
hw1/src/merge_report.py
